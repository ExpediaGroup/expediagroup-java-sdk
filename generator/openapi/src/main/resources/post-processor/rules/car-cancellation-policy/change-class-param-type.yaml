rule:
  kind: user_type
  pattern: $TYPE
  inside:
    any:
      - kind: class_parameter
        regex: freeCancellationEndDateTime
      - kind: nullable_type
        inside:
          kind: class_parameter
          regex: freeCancellationEndDateTime
